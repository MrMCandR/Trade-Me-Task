platform :ios, "13.0"
use_frameworks!
project 'Trade Me Task.xcodeproj'

def common_pods
  
  # Core Libraries
  pod 'Alamofire'
  pod 'Kingfisher'
  pod 'PKHUD'
  pod 'DZNEmptyDataSet'
  pod 'ImageSlideshow/Kingfisher'
  
end

target 'Trade Me Task' do
  common_pods
end

target 'Trade Me TaskTests' do
  common_pods
end
